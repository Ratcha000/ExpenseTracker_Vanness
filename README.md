#  Expense Tracker

โปรเจกต์นี้เป็นเว็บแอปพลิเคชันสำหรับ **บันทึกและแสดงผลค่าใช้จ่าย** พัฒนาด้วย **Node.js + Express + MongoDB (Atlas)** 
---
--> https://expensetracker-cqvl.onrender.com

## ความสามารถของระบบ (Features)

* เพิ่มข้อมูลค่าใช้จ่าย (Expense)
* แยกหมวดหมู่ค่าใช้จ่าย (Category)
* แสดงผลข้อมูลค่าใช้จ่ายในรูปแบบรายการ
* สรุปผลค่าใช้จ่ายเป็น Dashboard
* เชื่อมต่อ MongoDB Atlas
* Deploy ใช้งานจริงบน Render

---

## 🛠 Tech Stack

* **Backend:** Node.js, Express
* **Database:** MongoDB Atlas
* **ODM:** Mongoose
* **View Engine:** EJS
* **Deploy:** Render

---

##  โครงสร้างโปรเจกต์

```
Vanness_Expense/
│── app.js               
│── package.json
│── .env                  # (ไม่ push ขึ้น GitHub)
│── .gitignore
│
├── models/               # โครงสร้างข้อมูล (Schema)
│   ├── Expense.js
│   └── Category.js
│
├── routes/               # Router / API
│   ├── expenseRoutes.js
│   └── dashboardRoutes.js
│
├── views/                
│   ├── dashboard.ejs
│   ├── expenseForm.ejs
│   └── layout.ejs
│
├── public/               
              
```

---

##  การทำงานของระบบ (Concept)

### 1️ Model (Mongoose Schema)

#### Expense Model

```js
{
  title: String,
  amount: Number,
  category: ObjectId (ref: Category),
  createdAt: Date
}
```

ใช้สำหรับเก็บข้อมูลค่าใช้จ่ายแต่ละรายการ

#### Category Model

```js
{
  name: String
}
```

ใช้เก็บหมวดหมู่ เช่น อาหาร, เดินทาง, ของใช้

---

### 2️ Router (Controller Logic)

#### expenseRoutes.js

จัดการเกี่ยวกับค่าใช้จ่าย

| Method | Path          | Description                 |
| ------ | ------------- | --------------------------- |
| GET    | /expense/form | แสดงฟอร์มเพิ่มค่าใช้จ่าย    |
| POST   | /expense/add  | เพิ่มข้อมูลค่าใช้จ่าย       |
| GET    | /expenses     | แสดงรายการค่าใช้จ่ายทั้งหมด |

---

#### dashboardRoutes.js

จัดการหน้า Dashboard

| Method | Path       | Description        |
| ------ | ---------- | ------------------ |
| GET    | /dashboard | แสดงสรุปค่าใช้จ่าย |

ภายในใช้ MongoDB Aggregate เพื่อสรุปยอดตามหมวดหมู่

---

##  การรันโปรเจกต์บนเครื่อง

```bash
npm install
npm start
```

จากนั้นเข้า

```
http://localhost:3000/dashboard
```

Render Free Plan จะ sleep หากไม่มีผู้ใช้งาน

---





